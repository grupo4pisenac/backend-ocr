services:
  - type: web
    name: backend-ocr
    runtime: python
    buildCommand: apt-get install -y tesseract-ocr tesseract-ocr-por && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT